{
  "buildCommand": "npm install",
  "outputDirectory": ".",
  "env": {
    "OPENAI_API_KEY": {
      "placeholder": "sk-..."
    },
    "REPLICATE_API_TOKEN": {
      "placeholder": "r8_..."
    },
    "YOUTUBE_AUTH_KEY": {
      "placeholder": "{...}"
    }
  },
  "crons": [{
    "path": "/api/daily-upload",
    "schedule": "0 9 * * *"
  }]
}
